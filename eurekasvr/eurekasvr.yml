example:
  property: "This is default property"

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0    # 服务器接收请求之前等待的初始时间，5min

# 设置日志级别
logging:
  level:
    cn.qixqi.pan: DEBUG